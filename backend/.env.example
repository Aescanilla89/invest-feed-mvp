DATABASE_URL=sqlite:///./invest_feed.db
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
CORS_ALLOW_ORIGINS=["http://localhost:3000"]

# Alpaca Markets API (https://alpaca.markets → crea cuenta → Paper Trading → API Keys)
# Con estas claves el screener puede correr en Railway (sin bloqueo Yahoo Finance).
# Sin ellas, el screener usa yfinance (solo funciona en local).
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
